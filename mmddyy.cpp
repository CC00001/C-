#include <iostream>
#include <string>
using namespace std;

bool passwordCheck(char *p)
{
 while(*p != '/0')
 {
  if(strlen(p) != 10)
  {
   return false;
  }
  else if(isalpha(p[0] && islower(p[0])
  {
   return false;
  }
 }
}

int main()
{
 const int SIZE = 11;
 char passwords[SIZE];

 cout <<  
}
